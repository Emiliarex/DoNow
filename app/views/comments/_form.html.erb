<%= form_for [@category, @todo, step, step.comments.build], remote: true do |f| %>


  <%= f.label :body %>
<%= f.text_area :body, class: 'form-control', placeholder: "Enter Comment Body" %>

<div class="form-group">
  <%= f.submit "Save", class: 'btn btn-success' %>
</div>
<% end %>